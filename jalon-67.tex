\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{geometry}
\geometry{hmargin=2.5cm,vmargin=3cm}
\usepackage{tikz}
\usetikzlibrary{cd,positioning,shapes}
\usepackage{listings}
\usepackage{xcolor}

\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    stringstyle=\color{red},
    commentstyle=\color{gray}\itshape,
    numbers=left,
    numberstyle=\tiny\color{gray},
    breaklines=true,
    frame=single,
    showstringspaces=false,
    literate={é}{{\'e}}1 {è}{{\`e}}1 {à}{{\`a}}1 {ê}{{\^e}}1 {î}{{\^i}}1 {ç}{{\c{c}}}1 {ï}{{\"i}}1 {É}{{\'E}}1
}

\title{\Huge THÉORÈME DE CONVERGENCE MONOTONE (BEPPO LEVI) \\ \large Cours magistral, exercices corrigés et travaux pratiques}
\author{\Large Charles EDOU NZE}
\date{\today}

\begin{document}
\maketitle
\tableofcontents
\newpage

\part{Cours Magistral}

\section*{Jalon 67 : Théorème de convergence monotone (Beppo Levi)}

\subsection*{Présentation du concept clé}

- \textbf{La Métaphore :} Imaginez que vous construisiez une tour de blocs de glace qui fondent très lentement, mais que chaque jour vous ajoutiez une petite couche de glace supplémentaire par-dessus ($f_n \le f_{n+1}$). Vous voulez savoir quel sera le volume final de la tour ($f = \lim f_n$). Le \textbf{Théorème de Convergence Monotone} dit quelque chose de très simple et rassurant : le volume de la tour finale est exactement égal à la limite des volumes que vous avez mesurés jour après jour. En d'autres termes, pour des objets qui ne font que grandir, l'ordre dans lequel on fait les opérations (calculer le volume puis faire la limite, ou faire la limite puis calculer le volume) ne change pas le résultat.
- \textbf{Le "Pourquoi on a inventé ça" :} C'est la grande force de l'intégrale de Lebesgue par rapport à celle de Riemann. Avec Riemann, on ne pouvait pas garantir que la limite d'une suite de fonctions intégrables soit encore intégrable. Avec Lebesgue et Beppo Levi, on a un outil ultra-robuste pour manipuler les limites et les sommes infinies.
- \textbf{Visualisation :} Une suite de courbes qui "montent" vers une courbe plafond. L'aire sous les courbes monte elle aussi vers l'aire sous le plafond.

\subsection*{Formalisation}

Soit $(X, \mathcal{F}, \mu)$ un espace mesuré.

\subsubsection*{Énoncé du Théorème}

> \textbf{Théorème de Convergence Monotone (Beppo Levi) :}
> Soit $(f_n)_{n \in \mathbb{N}}$ une suite de fonctions mesurables de $X$ dans $[0, +\infty]$.
> Si la suite est \textbf{croissante} presque partout :
> $$\forall n \in \mathbb{N}, \quad f_n \le f_{n+1} \text{ p.p.}$$
> Alors la fonction limite $f = \lim_{n \to \infty} f_n$ est mesurable et :
> $$\int_X \left( \lim_{n \to \infty} f_n \right) d\mu = \lim_{n \to \infty} \int_X f_n d\mu$$

\subsubsection*{Corollaire (Sommation terme à terme)}

> \textbf{Théorème :} Pour toute suite de fonctions mesurables \textbf{positives} $(u_n)$ :
> $$\int_X \left( \sum_{n=0}^\infty u_n \right) d\mu = \sum_{n=0}^\infty \int_X u_n d\mu$$

\subsection*{Démonstrations}

\subsubsection*{Démonstration du Théorème de Beppo Levi}

1. \textbf{Existence de la limite :} Comme $(f_n(x))$ est une suite croissante de $[0, +\infty]$, elle admet toujours une limite dans $[0, +\infty]$ pour chaque $x$. On a vu (Jalon 65) que le sup (ou la limite ici) de fonctions mesurables est mesurable.
2. \textbf{Inégalité facile ($\ge$) :} Comme $f_n \le f$ pour tout $n$, par croissance de l'intégrale (Jalon 66) :
   $\int f_n \le \int f$. En prenant la limite : $\lim \int f_n \le \int f$.
3. \textbf{Inégalité difficile ($\le$) :} Soit $s$ une fonction simple telle que $0 \le s \le f$. Soit $\alpha \in ]0, 1[$.
   On définit $A_n = \{x \in X \mid f_n(x) \ge \alpha s(x) \}$.
   - Comme $f_n$ croît vers $f$ et $\alpha s < f$ (là où $s>0$), la suite d'ensembles $(A_n)$ est croissante et son union est $X$.
   - On a $\int f_n \ge \int_{A_n} f_n \ge \int_{A_n} \alpha s = \alpha \int_{A_n} s$.
   - Par continuité monotone de la mesure (Jalon 63), $\lim \int_{A_n} s = \int_X s$.
   - Donc $\lim \int f_n \ge \alpha \int_X s$.
   - En faisant tendre $\alpha \to 1$, on a $\lim \int f_n \ge \int s$.
4. \textbf{Conclusion :} Comme c'est vrai pour tout $s \le f$, alors $\lim \int f_n \ge \sup \int s = \int f$.
   Les deux inégalités prouvent l'égalité.

\subsection*{Exercices d'Application}

\subsubsection*{Exercice 1 : Intégrale d'une série}
\textbf{Énoncé :} Calculer $\int_0^1 \sum_{n=1}^\infty x^n dx$.
\textbf{Correction Détaillée :}
1. Les fonctions $u_n(x) = x^n$ sont mesurables et positives sur $[0, 1]$.
2. Par le corollaire du TCM, on peut intervertir somme et intégrale.
3. $I = \sum_{n=1}^\infty \int_0^1 x^n dx = \sum_{n=1}^\infty \left[ \frac{x^{n+1}}{n+1} \right]_0^1 = \sum_{n=1}^\infty \frac{1}{n+1}$.
4. Cette série est la série harmonique (moins son premier terme), elle diverge vers $+\infty$.
5. L'intégrale de la somme est donc $+\infty$.

\subsubsection*{Exercice 2 : Niveau Avancé (Utilisation de la mesure de comptage)}
\textbf{Énoncé :} Retrouver le théorème de sommation des séries à termes positifs doubles : $\sum_{i} \sum_{j} a_{i,j} = \sum_{j} \sum_{i} a_{i,j}$.
\textbf{Correction Détaillée :}
On considère l'espace $\mathbb{N}$ muni de la mesure de comptage $\mu$. Soit $f_n(i) = \sum_{j=0}^n a_{i,j}$. La suite de fonctions $(f_n)$ est croissante car $a_{i,j} \ge 0$. Par Beppo Levi, l'intégrale de la limite est la limite des intégrales, ce qui correspond exactement à l'interversion des sommes.

\subsection*{Application en Intelligence Artificielle}

- \textbf{Le Pont Théorique :} En IA, on manipule souvent des \textbf{Espérances de sommes infinies} ou des limites de processus. Le TCM est l'outil qui permet de passer à la limite sous l'espérance en toute sécurité.
- \textbf{Example Concret :}
    - \textbf{Processus de Poisson :} Pour calculer le nombre moyen d'événements (ex: clics sur une pub) sur un intervalle de temps, on somme les probabilités d'événements infinitésimaux. Le TCM garantit que la somme de ces moyennes locales donne bien la moyenne globale.
    - \textbf{Séries de Taylor de fonctions de perte :} Si on décompose une fonction de coût complexe en une série de fonctions positives, on peut intégrer cette série terme à terme pour obtenir une approximation de la perte attendue.
    - \textbf{Théorie des Noyaux (Kernels) :} De nombreux noyaux (comme le noyau RBF) peuvent être vus comme des sommes infinies de caractéristiques. Le TCM permet de manipuler ces représentations de dimension infinie comme si elles étaient finies lors des calculs d'intégrales de risque.

\subsection*{Liens Sémantiques}

- \textbf{Concepts Précédents requis :} [[Jalon 66 (Construction de l'intégrale de Lebesgue pour les fonctions mesurables positives.).md]], [[Jalon 63 (Définition axiomatique d'une mesure).md]]
- \textbf{Concepts Futurs dépendants :} [[Jalon 68 (Lemme de Fatou et définition de l'intégrale pour les fonctions de signe quelconque).md]], [[Jalon 69 (Démonstration complète du théorème de convergence dominée de Lebesgue.).md]]

\part{Exercices d'Application et de Concours}
\section*{Exercice 1 : Application du Théorème de Convergence Monotone 1}

\subsection*{Énoncé}
Soit $f_n(x) = \left(1 - \frac{x}{n}\right)^n \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\star\star\star\star$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = e^{-x}$. Soit $f(x) = e^{-x}$.
3. \textbf{Étape 3 :} La suite $f_n$ est croissante (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty e^{-x} dx = 1$.


\section*{Exercice 2 : Application du Théorème de Convergence Monotone 2}

\subsection*{Énoncé}
Soit $f_n(x) = \frac{x^2}{1 + n x^2} \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\star\star\star$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = 0$. Soit $f(x) = 0$.
3. \textbf{Étape 3 :} Ici on utilise plutôt la convergence dominée ou un corollaire adapté pour les séries (variante du problème). (Note: l'exercice est une variante de manipulation de limites). (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty 0 dx = 1$.


\section*{Exercice 3 : Application du Théorème de Convergence Monotone 3}

\subsection*{Énoncé}
Soit $f_n(x) = \left(1 - \frac{x}{n}\right)^n \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\bigstar\star\star$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = e^{-x}$. Soit $f(x) = e^{-x}$.
3. \textbf{Étape 3 :} La suite $f_n$ est croissante (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty e^{-x} dx = 1$.


\section*{Exercice 4 : Application du Théorème de Convergence Monotone 4}

\subsection*{Énoncé}
Soit $f_n(x) = \frac{x^4}{1 + n x^2} \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\bigstar\bigstar\star$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = 0$. Soit $f(x) = 0$.
3. \textbf{Étape 3 :} Ici on utilise plutôt la convergence dominée ou un corollaire adapté pour les séries (variante du problème). (Note: l'exercice est une variante de manipulation de limites). (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty 0 dx = 1$.


\section*{Exercice 5 : Application du Théorème de Convergence Monotone 5}

\subsection*{Énoncé}
Soit $f_n(x) = \left(1 - \frac{x}{n}\right)^n \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\bigstar\bigstar\bigstar$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = e^{-x}$. Soit $f(x) = e^{-x}$.
3. \textbf{Étape 3 :} La suite $f_n$ est croissante (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty e^{-x} dx = 1$.


\section*{Exercice 6 : Application du Théorème de Convergence Monotone 6}

\subsection*{Énoncé}
Soit $f_n(x) = \frac{x^6}{1 + n x^2} \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\bigstar\bigstar\bigstar$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = 0$. Soit $f(x) = 0$.
3. \textbf{Étape 3 :} Ici on utilise plutôt la convergence dominée ou un corollaire adapté pour les séries (variante du problème). (Note: l'exercice est une variante de manipulation de limites). (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty 0 dx = 1$.


\section*{Exercice 7 : Application du Théorème de Convergence Monotone 7}

\subsection*{Énoncé}
Soit $f_n(x) = \left(1 - \frac{x}{n}\right)^n \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\bigstar\bigstar\bigstar$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = e^{-x}$. Soit $f(x) = e^{-x}$.
3. \textbf{Étape 3 :} La suite $f_n$ est croissante (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty e^{-x} dx = 1$.


\section*{Exercice 8 : Application du Théorème de Convergence Monotone 8}

\subsection*{Énoncé}
Soit $f_n(x) = \frac{x^8}{1 + n x^2} \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\bigstar\bigstar\bigstar$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = 0$. Soit $f(x) = 0$.
3. \textbf{Étape 3 :} Ici on utilise plutôt la convergence dominée ou un corollaire adapté pour les séries (variante du problème). (Note: l'exercice est une variante de manipulation de limites). (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty 0 dx = 1$.


\section*{Exercice 9 : Application du Théorème de Convergence Monotone 9}

\subsection*{Énoncé}
Soit $f_n(x) = \left(1 - \frac{x}{n}\right)^n \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\bigstar\bigstar\bigstar$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = e^{-x}$. Soit $f(x) = e^{-x}$.
3. \textbf{Étape 3 :} La suite $f_n$ est croissante (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty e^{-x} dx = 1$.


\section*{Exercice 10 : Application du Théorème de Convergence Monotone 10}

\subsection*{Énoncé}
Soit $f_n(x) = \frac{x^10}{1 + n x^2} \mathbf{1}_{[0, n]}(x)$ pour $x \ge 0$.
Étudier la limite de $\int_0^n f_n(x) dx$ quand $n \to \infty$.

*Difficulté:* $\bigstar\bigstar\bigstar\bigstar\bigstar$

\subsection*{Correction Détaillée}
1. \textbf{Étape 1 :} Pour un $x \ge 0$ fixé, dès que $n > x$, $f_n(x) = (1 - x/n)^n$.
2. \textbf{Étape 2 :} On sait que $\lim_{n \to \infty} (1 - x/n)^n = 0$. Soit $f(x) = 0$.
3. \textbf{Étape 3 :} Ici on utilise plutôt la convergence dominée ou un corollaire adapté pour les séries (variante du problème). (Note: l'exercice est une variante de manipulation de limites). (on peut le prouver en dérivant par rapport à $n$).
4. \textbf{Étape 4 :} On applique le TCM de Beppo Levi.
5. \textbf{Conclusion :} $\lim \int_0^n f_n(x) dx = \int_0^\infty 0 dx = 1$.



\part{Travaux Pratiques et Simulations Algorithmiques}
\section*{TP 1 : Simulation Numérique du Théorème de Beppo Levi}

Ce TP illustre la convergence monotone en calculant numériquement l'intégrale d'une suite croissante de fonctions.

\begin{lstlisting}
# TP 1 - Implementation from scratch
import math

def f_n(x, n):
    # Suite croissante de fonctions: f_n(x) = x * (1 - 1/n) (variante pour le TP 1)
    return (x**1) * (1.0 - 1.0/n)

def riemann_sum(f, n, a, b, num_steps):
    step = (b - a) / num_steps
    total = 0.0
    for j in range(num_steps):
        x = a + j * step
        total += f(x, n) * step
    return total

a = 0.0
b = 1.0
num_steps = 1000

print("--- Simulation for n values ---")
for n in [2, 10, 100, 1000]:
    integral_n = riemann_sum(f_n, n, a, b, num_steps)
    print(f"n={n}, Integral = {integral_n:.5f}")

# Expected limit for f(x) = x**1 on [0, 1] is 1/(1+1)
expected_limit = 1.0 / (1 + 1.0)
print(f"Expected limit (n -> infinity): {expected_limit:.5f}")
\end{lstlisting}


\section*{TP 2 : Simulation Numérique du Théorème de Beppo Levi}

Ce TP illustre la convergence monotone en calculant numériquement l'intégrale d'une suite croissante de fonctions.

\begin{lstlisting}
# TP 2 - Implementation from scratch
import math

def f_n(x, n):
    # Suite croissante de fonctions: f_n(x) = x * (1 - 1/n) (variante pour le TP 2)
    return (x**2) * (1.0 - 1.0/n)

def riemann_sum(f, n, a, b, num_steps):
    step = (b - a) / num_steps
    total = 0.0
    for j in range(num_steps):
        x = a + j * step
        total += f(x, n) * step
    return total

a = 0.0
b = 1.0
num_steps = 1000

print("--- Simulation for n values ---")
for n in [2, 10, 100, 1000]:
    integral_n = riemann_sum(f_n, n, a, b, num_steps)
    print(f"n={n}, Integral = {integral_n:.5f}")

# Expected limit for f(x) = x**2 on [0, 1] is 1/(2+1)
expected_limit = 1.0 / (2 + 1.0)
print(f"Expected limit (n -> infinity): {expected_limit:.5f}")
\end{lstlisting}


\section*{TP 3 : Simulation Numérique du Théorème de Beppo Levi}

Ce TP illustre la convergence monotone en calculant numériquement l'intégrale d'une suite croissante de fonctions.

\begin{lstlisting}
# TP 3 - Implementation from scratch
import math

def f_n(x, n):
    # Suite croissante de fonctions: f_n(x) = x * (1 - 1/n) (variante pour le TP 3)
    return (x**3) * (1.0 - 1.0/n)

def riemann_sum(f, n, a, b, num_steps):
    step = (b - a) / num_steps
    total = 0.0
    for j in range(num_steps):
        x = a + j * step
        total += f(x, n) * step
    return total

a = 0.0
b = 1.0
num_steps = 1000

print("--- Simulation for n values ---")
for n in [2, 10, 100, 1000]:
    integral_n = riemann_sum(f_n, n, a, b, num_steps)
    print(f"n={n}, Integral = {integral_n:.5f}")

# Expected limit for f(x) = x**3 on [0, 1] is 1/(3+1)
expected_limit = 1.0 / (3 + 1.0)
print(f"Expected limit (n -> infinity): {expected_limit:.5f}")
\end{lstlisting}


\section*{TP 4 : Simulation Numérique du Théorème de Beppo Levi}

Ce TP illustre la convergence monotone en calculant numériquement l'intégrale d'une suite croissante de fonctions.

\begin{lstlisting}
# TP 4 - Implementation from scratch
import math

def f_n(x, n):
    # Suite croissante de fonctions: f_n(x) = x * (1 - 1/n) (variante pour le TP 4)
    return (x**4) * (1.0 - 1.0/n)

def riemann_sum(f, n, a, b, num_steps):
    step = (b - a) / num_steps
    total = 0.0
    for j in range(num_steps):
        x = a + j * step
        total += f(x, n) * step
    return total

a = 0.0
b = 1.0
num_steps = 1000

print("--- Simulation for n values ---")
for n in [2, 10, 100, 1000]:
    integral_n = riemann_sum(f_n, n, a, b, num_steps)
    print(f"n={n}, Integral = {integral_n:.5f}")

# Expected limit for f(x) = x**4 on [0, 1] is 1/(4+1)
expected_limit = 1.0 / (4 + 1.0)
print(f"Expected limit (n -> infinity): {expected_limit:.5f}")
\end{lstlisting}


\section*{TP 5 : Simulation Numérique du Théorème de Beppo Levi}

Ce TP illustre la convergence monotone en calculant numériquement l'intégrale d'une suite croissante de fonctions.

\begin{lstlisting}
# TP 5 - Implementation from scratch
import math

def f_n(x, n):
    # Suite croissante de fonctions: f_n(x) = x * (1 - 1/n) (variante pour le TP 5)
    return (x**5) * (1.0 - 1.0/n)

def riemann_sum(f, n, a, b, num_steps):
    step = (b - a) / num_steps
    total = 0.0
    for j in range(num_steps):
        x = a + j * step
        total += f(x, n) * step
    return total

a = 0.0
b = 1.0
num_steps = 1000

print("--- Simulation for n values ---")
for n in [2, 10, 100, 1000]:
    integral_n = riemann_sum(f_n, n, a, b, num_steps)
    print(f"n={n}, Integral = {integral_n:.5f}")

# Expected limit for f(x) = x**5 on [0, 1] is 1/(5+1)
expected_limit = 1.0 / (5 + 1.0)
print(f"Expected limit (n -> infinity): {expected_limit:.5f}")
\end{lstlisting}



\end{document}
