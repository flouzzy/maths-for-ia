## TP 2 : Fonctions indicatrices et mesures

**Objectif :** Implémenter le calcul de l'intégrale d'une fonction simple définie par des indicatrices.

```python
class SimpleFunction:
    def __init__(self):
        self.coefficients = []
        self.sets = []

    def add_term(self, coeff, intervals):
        self.coefficients.append(coeff)
        self.sets.append(intervals)

    def evaluate(self, x):
        val = 0
        for i in range(len(self.coefficients)):
            in_set = False
            for interval in self.sets[i]:
                if interval[0] <= x < interval[1]:
                    in_set = True
                    break
            if in_set:
                val += self.coefficients[i]
        return val

    def integrate_lebesgue(self):
        total = 0
        for i in range(len(self.coefficients)):
            measure = 0
            for interval in self.sets[i]:
                measure += (interval[1] - interval[0])
            total += self.coefficients[i] * measure
        return total

if __name__ == "__main__":
    s = SimpleFunction()
    s.add_term(2, [[0, 1]])
    s.add_term(5, [[2, 3]])

    integral = s.integrate_lebesgue()
    print("Integral of simple function:", integral)
    assert integral == 7
```
