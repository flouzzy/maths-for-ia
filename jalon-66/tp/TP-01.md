## TP 1 : Approximation de l'intégrale de Lebesgue

**Objectif :** Écrire un script Python pur pour approcher l'intégrale de Lebesgue d'une fonction positive en utilisant des fonctions simples, sans aucune bibliothèque externe.

```python
def lebesgue_integral_approximation(func, domain_start, domain_end, num_points, num_levels):
    step_size = (domain_end - domain_start) / num_points
    domain = [domain_start + j * step_size for j in range(num_points)]

    values = [func(x) for x in domain]
    max_val = max(values)

    if max_val == 0:
        return 0.0

    level_step = max_val / num_levels
    integral = 0.0

    for i in range(num_levels):
        current_level = i * level_step
        measure = 0.0
        for val in values:
            if val >= current_level:
                measure += step_size

        integral += level_step * measure

    return integral

def f(x):
    return x * x

if __name__ == "__main__":
    result = lebesgue_integral_approximation(f, 0, 1, 1000, 100)
    print("Approximation for f(x) = x*x on [0, 1]:", result)
    diff = result - (1.0 / 3.0)
    if diff < 0:
        diff = -diff
    assert diff < 0.05
```
