# TP 5 : Contre-exemple de la suite non croissante (bosse glissante)

## Objectif
Implémentation algorithmique sans bibliothèques externes pour valider les comportements asymptotiques étudiés.

## Implémentation

```python
def f(n, x):
    if 0 < x <= 1.0/n:
        return n
    return 0

# Integral de f_n sur R est toujours 1
def integral_f_n():
    return 1.0

# Limite simple de f_n(x) pour x > 0 est 0
def integral_limite():
    return 0.0

assert integral_f_n() != integral_limite()
print("Le theoreme echoue car la suite n'est pas croissante")
```
