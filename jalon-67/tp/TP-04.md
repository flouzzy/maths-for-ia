# TP 4 : Fonction plateau

Dans ce TP numéro 4, nous vérifions numériquement l'interversion de Beppo Levi.

```python
def integrate_func(f, a, b, num_steps=100):
    step_size = (b - a) / num_steps
    return sum(f(a + k * step_size) * step_size for k in range(num_steps))

def fn(n, x):
    # Definition of f_n
    pass

def main():
    print("Execution du TP 4")
    val_1000 = integrate_func(lambda x: x, 0.0, 1.0)
    assert val_1000 >= 0, "Integrale positive"

if __name__ == "__main__":
    main()
```
