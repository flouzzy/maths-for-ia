---
uuid: "tp-21-05"
title: "TP 5 : Validation du théorème de Dini"
---

# TP 5 : Validation du théorème de Dini

**Objectif :** Implémenter une suite de fonctions continues décroissant vers une limite continue sur un maillage discret. Écrire un script pour trouver le $N$ tel que pour tout $n \geq N$, l'erreur maximale soit inférieure à $\epsilon$, validant la convergence uniforme.

## Implémentation Pure Python (Validation Mathématique)

```python
def f_n(n, x):
    return 1 / (1 + n * x)

# Sur [a, 1] avec a > 0, la suite converge uniformement vers 0.
# Verifions que pour eps > 0 donne, on peut trouver un N global.
def trouver_N_uniforme(eps, a, b, num_points=1000):
    pas = (b - a) / num_points
    points = [a + i*pas for i in range(num_points + 1)]

    n = 1
    while True:
        max_err = 0
        for x in points:
            err = abs(f_n(n, x))
            if err > max_err: max_err = err

        if max_err < eps:
            return n
        n += 1

a = 0.1
b = 1.0
eps = 0.05
N_requis = trouver_N_uniforme(eps, a, b)

# Validation : Pour n >= N_requis, max_err doit etre < eps
max_err_validation = 0
pas = (b - a) / 100
for i in range(101):
    x = a + i*pas
    err = abs(f_n(N_requis, x))
    if err > max_err_validation: max_err_validation = err
assert max_err_validation < eps
print(f"Theoreme de Dini valide : N trouve = {N_requis}")
```
